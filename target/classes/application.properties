# Select persistence backend: cache | database
repository.type=database

# Active environment: dev | test | prod (can override via -Dapp.env)
app.env=dev

# Default DB (fallback)
db.url=jdbc:mysql://localhost:3306/quantitymeasurement?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.username=root
db.password=sneha

# Development DB
dev.db.url=jdbc:mysql://localhost:3306/quantitymeasurement?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
dev.db.username=root
dev.db.password=sneha

# Test DB
test.db.url=jdbc:h2:mem:quantitymeasurement_test;DB_CLOSE_DELAY=-1;MODE=MySQL
test.db.username=sa
test.db.password=

# Production DB
prod.db.url=jdbc:mysql://localhost:3306/quantitymeasurement?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
prod.db.username=root
prod.db.password=sneha

# Connection pool
db.pool.initialSize=3
db.pool.maxSize=10
db.pool.acquireTimeoutMs=5000
